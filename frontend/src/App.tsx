import { AppRouter } from '@/app/providers/RouterProvider';
import './App.css';

/**
 * Root App component
 * Renders the main router with all pages
 */
function App() {
  return <AppRouter />;
}

export default App;
